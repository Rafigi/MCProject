language: CSharp
mono:
 - none
 dotnet: 2.1.502
 script:
 - dotnet restore
 install:
  - nuget restore MCProject.sln
  - nuget install NUnit.Console -Version 3.9.0 -OutputDirectory testrunner
script:
  - msbuild /p:Configuration=Release MCProject.sln
